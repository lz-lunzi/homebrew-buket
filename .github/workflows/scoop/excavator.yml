on:
  workflow_dispatch:
  schedule:
    # run daily at 00:00 China time (16:00 UTC previous day)
    - cron: '0 16 * * *'
  push:
    branches:
      - 'main'
      - 'master'
    paths:
      - '.github/workflows/scoop/excavator.yml'
name: Excavator
jobs:
  excavate:
    name: Excavate
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@main
      - name: Excavate
        uses: ScoopInstaller/GithubActions@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
